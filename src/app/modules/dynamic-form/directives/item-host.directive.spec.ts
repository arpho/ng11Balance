import { ItemHostDirective } from './item-host.directive';

describe('ItemHostDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemHostDirective();
    expect(directive).toBeTruthy();
  });
});
